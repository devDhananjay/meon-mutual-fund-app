/**
 * Shared vertical rhythm for main tab screens (Dashboard, Explore, My Folios, etc.)
 * so title + search sit at the same distance from the top / notch.
 */
export const TAB_SCREEN_SAFE_TOP_EXTRA = 12;
export const TAB_SCREEN_TITLE_TO_SEARCH = 12;
